export enum AuthTabEnum {
  Login = 'login',
  Register = 'register',
}

export enum ResponseStatus {
  OK = 'OK',
}

export enum FileTypeEnum {
  Image = 'image',
  Audio = 'audio',
  Pdf = 'pdf',
  Video = 'video',
}
